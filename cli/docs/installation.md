# Installation

The CLI requires Python 3.10+ and is distributed as part of the `migrator-gen` monorepo.

## From source

```bash
# Ensure migrator-gen SDK is installed
pip install -e sdk/python[all]

# Install CLI
pip install -e cli[dev]
```

## Verify

```bash
migrator-gen --version
```
