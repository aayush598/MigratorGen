# mylib_migrator

Auto-generated code migrator for **mylib**.

## Installation

```bash
pip install -e .
```

## Usage

```bash
# List available versions
python -m mylib_migrator list-versions

# Migrate a project
python -m mylib_migrator migrate --from 1.0.0 --to 2.0.0 ./my_project/

# Migrate to latest
python -m mylib_migrator migrate --from 1.0.0 --to latest ./my_project/

# Dry run (preview only)
python -m mylib_migrator migrate --from 1.0.0 --to latest ./my_project/ --dry-run

# Show diff preview
python -m mylib_migrator migrate --from 1.0.0 --to latest myfile.py --preview

# Downgrade
python -m mylib_migrator migrate --from 3.0.0 --to 1.0.0 ./my_project/
```

## Covered versions

- **v4.0.0** — 4 rule(s)
- **v4.1.0** — 2 rule(s)

---
*Generated by MigratorGen on 2026-04-22 16:26:30*
